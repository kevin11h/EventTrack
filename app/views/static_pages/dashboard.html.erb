<h1>
    Events I've created: <span class="pull-right"><%= link_to "My events", events_path, class: 'btn btn-success' %></span>
</h1>

<% if @events.any? %>
    <%= render 'shared/events', object: @events %>
<% else %>
    <h4 class="text-center text-muted">You haven't created any event, <%= link_to 'click here', new_event_path %> and create one now</h4> 
<% end %>
<hr class="featurette-divider">

<h1>
    Events I'm invited: <span class="pull-right"><%= link_to "My Invites", invites_path, class: 'btn btn-success' %></span> 
</h1>

<% if @invites.any? %>
    <%= render 'shared/invited_events', object: @invites %>
<% else %>
    <h4 class="text-center text-muted">You aren't going anywhere </h4>
<% end %>

